---
phase: build
classification: simple-workflow
module_code: formally-bmad
last_touched: 2026-05-30T16:23:00Z
---

# Decision Log

## 2026-05-30 Session

- Built `formally-bmad-setup` as a module-based simple workflow, not a complex workflow, because the setup flow is linear enough to keep in `SKILL.md`.
- Added one deterministic stdlib Python helper, `scripts/setup_environment.py`, for folder creation, artifact discovery, tool detection, smoke tests, and report scaffolding. This keeps filesystem/tool plumbing out of prompt instructions.
- Did not add `customize.toml`; this setup workflow already writes module configuration and does not need a separate skill-customization surface.
- Required at least one supported automated reasoning tool before setup succeeds, matching the module plan. Export-only fallback is limited to optional missing backends after minimum tool availability is met.
- Kept repairs as `confirm-before-edit` in the generated module config because accepted repairs can change requirement or architecture meaning.
- Wrote the setup skill to produce Markdown setup reports by default. HTML is listed in module config for later reporting skills, but this setup helper keeps first implementation dependency-free.
- Moved the decision log under `references/` because the path-standards scanner flags root-level `.decision-log.md` as progressive disclosure content, despite the builder process describing the log as a peer of `SKILL.md`.
