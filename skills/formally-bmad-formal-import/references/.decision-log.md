---
phase: build
classification: simple-workflow
module_code: formally-bmad
last_touched: 2026-05-30T16:50:00Z
---

# Decision Log

## 2026-05-30 Session

- Built `formally-bmad-formal-import` as a simple workflow because the import flow is linear and can stay in `SKILL.md`.
- Added `scripts/import_inventory.py` for deterministic file discovery, kind hints, import workspace creation, inventory JSON, and skeleton Markdown report.
- Kept semantic classification and formal candidate extraction in the workflow prompt. The script only handles plumbing and heuristics; it does not decide formal meaning.
- Import uses conservative assertion states. Existing artifacts enter as `candidate`, `provisional`, `open-question`, or `assumption`; `accepted` requires explicit user confirmation later.
- Import does not edit source BMad artifacts. Contradictions during import are blockers or repair candidates for a later repair workflow.
- The workflow depends on `formally-bmad-setup` for initialized project state and on `formally-bmad-agent-steward` for canonical delta acceptance.
